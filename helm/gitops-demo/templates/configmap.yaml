apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gitops-demo.fullname" . }}-config
data:
  APP_ENV: "production"
  DB_HOST: "postgres"
  DB_NAME: {{ .Values.postgres.database | quote }}
  APP_VERSION: {{ .Values.api.env.APP_VERSION | quote }}
